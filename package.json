{
  "name": "retrieval-skill",
  "version": "1.0.0",
  "description": "Generic retrieval system: incremental indexing + hybrid FTS5/cosine search",
  "type": "module",
  "bin": {
    "retrieve": "./src/cli.mjs"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.4.0",
    "better-sqlite3": "^11.8.0",
    "commander": "^13.1.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
